import React from 'react';
import { inject, observer } from 'mobx-react';
import WebBaseCpn from '../../../../common/components/web/WebBaseCpn';

export default class LoginScreen extends WebBaseCpn {

    constructor(props) {
        super(props);
    }

    render() {
        return (<div>login</div>);
    }

}